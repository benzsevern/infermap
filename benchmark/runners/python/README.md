# infermap-bench

Installable accuracy benchmark runner for the [infermap](../../../infermap/) package.

## Install (from repo root)

```bash
cd benchmark/runners/python
pip install -e ".[dev]"
```

## Usage

See the main benchmark README at `../../README.md` or the implementation plan for CLI reference.
